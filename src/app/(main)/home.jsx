import { ScrollView } from "react-native";
import WeatherCard from '../../components/weatherCard'

const Home=()=>{

   return(
     <ScrollView>

        <WeatherCard/>


    
    </ScrollView>
   )

}

export default Home;